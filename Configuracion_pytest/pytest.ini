[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --disable-warnings
    --strict-markers
markers =
    unit: Tests unitarios
    integration: Tests de integración
